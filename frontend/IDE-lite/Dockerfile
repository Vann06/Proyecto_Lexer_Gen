# Sirve los archivos estáticos del panel semántico con nginx. A diferencia
# de frontend/IDE-full/Dockerfile, no hace falta renombrar nada: el archivo
# principal ya se llama index.html.
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
