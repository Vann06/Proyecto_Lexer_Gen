{
(* header opcional *)
}

let digit = ['0'-'9']
let letter = ['a'-'z''A'-'Z']
let ws = [' ''\t''\n']

rule tokens =
    {ws}+        { /* ignore */ }
  | {letter}({letter}|{digit})* { ID }
  | {digit}+     { NUMBER }
  | '+'          { PLUS }
  | '-'          { MINUS }

{
(* trailer opcional *)
}